{
  "name": "polling-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Live timed polling backend (Node cluster + Postgres + Redis)",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "migrate": "node src/scripts/migrate.js",
    "seed": "node src/scripts/seed.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "ioredis": "^5.4.2",
    "pg": "^8.13.1"
  }
}
